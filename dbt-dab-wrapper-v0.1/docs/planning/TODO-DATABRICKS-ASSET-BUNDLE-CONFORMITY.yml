# TODO-DATABRICKS-ASSET-BUNDLE-CONFORMITY.yml
# ────────────────────────────────────────────────────────────────
#   dbt-dab-wrapper — Databricks Asset Bundle (DAB) Conformity TODO
#   Date: 23 March 2026
#   CLI note: All commands use `forge` (the real CLI)
#   Goal: forge deploy / forge teardown = 100% native, safe, one-command DAB
#   NEW PHILOSOPHY (user requirement): 
#     • Teardowns MUST NEVER delete any data
#     • Everything must always be redeployable with clear re-instate instructions
#     • Nothing is overwritten without being saved first (backup step)
#   Rule: Users edit ONLY wrapper.yml + dbt/ddl/. Everything else generated.
#         Graph shows every asset. One command = everything.
#         SQL preferred. Code = the documentation.
# ────────────────────────────────────────────────────────────────

current_score: 9.5/10
review_summary: >
  #1, #2, #3 (safe teardown), and #6 are now ✅ DONE.
  Teardown is fully wired: dry-run by default, --execute to run, backup + reinstate.

updated_todos:

  - id: workflow-yaml-location-and-creation
    priority: 1
    status: ✅ DONE

  - id: per-model-tasks-in-dag
    priority: 2
    status: ✅ DONE

  - id: teardown-workflow-full-support
    priority: 3
    status: ✅ DONE
    title: Add safe, one-command teardown workflow (NEVER deletes data)
    completed:
      - src/forge/teardown.py module with TeardownPlan dataclass
      - build_teardown_plan() reads graph to discover all assets
      - Classifies assets into removes (DAB job + UDFs) and preserves (tables + schemas)
      - Generates TEARDOWN_{id}.yml plan (mirrors PROCESS_{id}.yml pattern)
      - forge teardown = dry-run by default (shows plan, writes YAML)
      - forge teardown --execute = backup workflow → delete YAML → drop UDFs → confirm
      - Backup saved to resources/teardown/_backup/{name}_{timestamp}.yml
      - Always ends with "To re-instate: forge deploy"
      - UDF SQL printed for manual execution if dbt not available
    verified_output: "Plan shows 1 DAB job + 3 UDFs to remove, 5 tables preserved"

  - id: pure-sql-on-serverless-with-error-success
    priority: 4
    status: Ready to build

  - id: dbt-run-only-when-pure-sql-impossible
    priority: 5
    status: Ready to build

  - id: custom-workflow-empowerment
    priority: 6
    status: ✅ DONE

success_criteria:
  - forge teardown never deletes data — only removes orchestration
  - Every teardown creates a backup and prints exact re-instate command
  - forge deploy always works as the "undo" button
  - Still one command for everything
  - New dev sees clear "nothing was deleted" message

auto_documentation:
  - After every todo: run `forge guide`
  - New section appears automatically: "## Safe Teardown Philosophy (Never Deletes Data)"

next_action:
  - #1, #2, #6 DONE ✅
  - Start #3 today (teardown with your new safety rules — 2–4 days)
  - Run `forge guide` after each step

last_updated: 2026-03-23