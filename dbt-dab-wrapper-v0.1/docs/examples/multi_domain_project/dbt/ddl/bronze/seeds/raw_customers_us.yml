# ── US raw customer data ──────────
seeds:
  raw_customers_us:
    description: "Raw customer data for the US region"
    domain: false
    origin:
      type: seed
      path: dbt/seeds/raw_customers_us.csv
      format: csv
    columns:
      customer_id:  { type: int, description: "Unique customer identifier" }
      email:        { type: string }
      country:      { type: string }
      revenue:      { type: "decimal(10,2)" }
